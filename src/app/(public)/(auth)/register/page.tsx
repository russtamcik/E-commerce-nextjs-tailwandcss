import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Najot e-commerce | Register",
  description: "E-commerce website Register side",
};

const Register = () => {
  return <div style={{ marginTop: "100px" }}>Register</div>;
};

export default Register;
